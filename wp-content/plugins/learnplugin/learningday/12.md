# add custom JavaScript files to wp page

```php
<?php
wp_localize_script( string $handle, string $object_name, array $l10n ): bool
```

reference: https://developer.wordpress.org/reference/functions/wp_localize_script/

Key point: wp_localize_script() must reference a script handle that's already been enqueued with wp_enqueue_script().
